# Bài 18: Decision Tree (Cây Quyết Định)

> **Môn học:** Data Science với R  
> **Dữ liệu sử dụng:** `titanic.csv`, `wines.csv`, `german1.csv`, `medical_care.csv`  
> **Kiến thức yêu cầu:** Bài 17 — Cross-Validation

---

## 1. Động lực: Mô hình có thể giải thích được

Logistic Regression cho ta một công thức toán học — nhưng khó giải thích với người không có nền toán học. **Decision Tree** giải quyết vấn đề này: mô hình hoạt động giống hệt cách con người ra quyết định — đặt câu hỏi Yes/No theo từng bước.

**Ví dụ thực tế — dự đoán sống sót trên Titanic:**

```
                    [Là nữ không?]
                   /              \
                 Có               Không
                /                   \
         [Hạng vé ≥ 3?]       [Tuổi < 10?]
          /         \           /        \
        Có         Không      Có        Không
        /             \       /            \
    KHÔNG sống    SỐNG     SỐNG        KHÔNG sống
```

Cây trên có thể đọc và giải thích ngay — đây là **lợi thế lớn nhất** của Decision Tree so với các mô hình phức tạp hơn.

---

## 2. Cấu trúc của một Decision Tree

```
                    [Root Node]          ← Nút gốc: câu hỏi đầu tiên
                   /           \
            [Internal]       [Internal]  ← Nút trong: câu hỏi tiếp theo
            /      \         /      \
         [Leaf]  [Leaf]  [Leaf]  [Leaf]  ← Nút lá: kết quả dự đoán
```

**Các thành phần:**
- **Root Node:** Nút gốc — biến và ngưỡng phân chia đầu tiên (quan trọng nhất).
- **Internal Node:** Nút trong — tiếp tục đặt câu hỏi phân chia.
- **Leaf Node (Terminal Node):** Nút lá — trả về nhãn lớp (phân loại) hoặc giá trị trung bình (hồi quy).
- **Branch:** Nhánh — kết quả của một phân chia (Yes/No, ≤ threshold / > threshold).
- **Depth:** Độ sâu — số tầng tính từ root đến leaf xa nhất.

---

## 3. Thuật toán CART

**CART (Classification and Regression Trees)** là thuật toán phổ biến nhất để xây dựng Decision Tree, được sử dụng trong package `rpart` của R.

### 3.1. Nguyên lý: Greedy Top-Down Splitting

CART xây dựng cây theo chiến lược **tham lam (greedy)**: tại mỗi nút, chọn cặp *(biến, ngưỡng)* tốt nhất để phân chia, không quan tâm đến các bước sau.

**Quy trình tại mỗi nút:**
1. Duyệt qua **tất cả** biến $X_1, X_2, \ldots, X_p$.
2. Với mỗi biến, duyệt qua **tất cả** ngưỡng phân chia có thể.
3. Chọn cặp *(biến, ngưỡng)* **giảm impurity nhiều nhất**.
4. Dừng khi đạt điều kiện dừng (nút lá đủ nhỏ, hoặc độ sâu tối đa).

### 3.2. Đo lường chất lượng phân chia — Classification

**Gini Impurity** (mặc định trong `rpart`):

$$\text{Gini}(t) = 1 - \sum_{k=1}^{K} p_k^2$$

trong đó $p_k$ là tỷ lệ lớp $k$ tại nút $t$.

- $\text{Gini} = 0$: Nút **thuần khiết** hoàn toàn (chỉ có 1 lớp).
- $\text{Gini} = 0.5$: Nút **hỗn loạn** nhất (2 lớp bằng nhau, 50/50).

**Entropy (Information Gain):**

$$\text{Entropy}(t) = -\sum_{k=1}^{K} p_k \log_2(p_k)$$

**Mức giảm impurity sau phân chia:**

$$\Delta \text{Impurity} = \text{Impurity}(t) - \frac{N_L}{N} \cdot \text{Impurity}(t_L) - \frac{N_R}{N} \cdot \text{Impurity}(t_R)$$

CART chọn phân chia **tối đa hóa** $\Delta \text{Impurity}$.

**Ví dụ tính Gini:**

```
Nút cha: 10 quan sát (6 Yes, 4 No)
  Gini(cha) = 1 - (0.6² + 0.4²) = 1 - (0.36 + 0.16) = 0.48

Phân chia A:
  Nút trái:  7 obs (6 Yes, 1 No) → Gini = 1-(6/7)²-(1/7)² = 0.245
  Nút phải:  3 obs (0 Yes, 3 No) → Gini = 1-(0)²-(1)²    = 0.000
  Gini(A) = (7/10)×0.245 + (3/10)×0.000 = 0.172
  ΔGini(A) = 0.48 - 0.172 = 0.308  ✅

Phân chia B:
  Nút trái:  5 obs (4 Yes, 1 No) → Gini = 1-(4/5)²-(1/5)² = 0.320
  Nút phải:  5 obs (2 Yes, 3 No) → Gini = 1-(2/5)²-(3/5)² = 0.480
  Gini(B) = (5/10)×0.320 + (5/10)×0.480 = 0.400
  ΔGini(B) = 0.48 - 0.400 = 0.080

→ Chọn phân chia A (ΔGini lớn hơn)
```

### 3.3. Đo lường chất lượng phân chia — Regression

Với **Regression Tree**, thay Gini bằng **Residual Sum of Squares (RSS)**:

$$\text{RSS}(t) = \sum_{i \in t} (y_i - \bar{y}_t)^2$$

Chọn phân chia **tối thiểu hóa** tổng RSS của hai nút con:

$$\min_{j, s} \left[ \text{RSS}(t_L) + \text{RSS}(t_R) \right]$$

Giá trị dự đoán tại mỗi leaf = **trung bình** $\bar{y}$ của các quan sát trong leaf đó.

---

## 4. Overfitting và Pruning (Cắt tỉa cây)

### 4.1. Vấn đề Overfitting

Nếu để cây phát triển tự do cho đến khi mỗi leaf chỉ có 1 quan sát → **Training accuracy = 100%** nhưng mô hình hoàn toàn vô dụng trên dữ liệu mới.

```
Cây quá sâu (Overfitting):          Cây quá nông (Underfitting):
Khớp hoàn hảo với training data     Bỏ qua nhiều quy luật quan trọng
→ Test error cao                     → Cả train lẫn test error đều cao
```

### 4.2. Pre-pruning (Dừng sớm)

Kiểm soát độ phức tạp **trong khi** xây dựng cây, qua các tham số:

| Tham số trong `rpart` | Ý nghĩa | Giá trị mặc định |
|---|---|---|
| `maxdepth` | Độ sâu tối đa | 30 |
| `minsplit` | Số quan sát tối thiểu tại nút để tiếp tục chia | 20 |
| `minbucket` | Số quan sát tối thiểu tại nút lá | `minsplit/3` |
| `cp` (complexity parameter) | Mức cải thiện tối thiểu để thêm nút | 0.01 |

**Complexity Parameter (cp):** Phân chia chỉ được thực hiện nếu:

$$\Delta \text{Impurity} \geq cp \times \text{Impurity(root)}$$

`cp` cao → cây nhỏ hơn (đơn giản hơn). `cp = 0` → cây phát triển tự do.

### 4.3. Post-pruning (Cắt tỉa sau)

Xây cây **đầy đủ** trước, sau đó cắt bỏ các nhánh không cải thiện đủ trên **validation data**. CART dùng **Cost-Complexity Pruning**:

$$R_\alpha(T) = R(T) + \alpha \cdot |T|$$

trong đó:
- $R(T)$: Training error của cây $T$
- $|T|$: Số nút lá (đo độ phức tạp)
- $\alpha$: Tham số điều chỉnh — tăng $\alpha$ → phạt cây phức tạp hơn → cây nhỏ hơn

**Trong thực hành:** Dùng Cross-Validation để chọn `cp` tối ưu — đây chính là post-pruning trong `rpart`.

---

## 5. So sánh với Linear/Logistic Regression

| Tiêu chí | Decision Tree | Linear/Logistic Regression |
|---|---|---|
| Dạng ranh giới | Trực giao (axis-aligned) | Tuyến tính |
| Quan hệ phi tuyến | ✅ Tự động nắm bắt | ❌ Cần feature engineering |
| Tương tác biến | ✅ Tự động | ❌ Cần thêm thủ công |
| Outlier | Khá bền vững | Nhạy cảm hơn |
| Cần chuẩn hóa dữ liệu | ❌ Không cần | ✅ Cần (với regularization) |
| Giải thích được | ✅ Rất tốt | ✅ Tốt (với hệ số) |
| Ổn định | ❌ Variance cao | ✅ Ổn định hơn |
| Hiệu năng tổng thể | Trung bình (đơn lẻ) | Tốt với dữ liệu tuyến tính |
| Nền tảng cho Ensemble | ✅ (Bài 19, 20) | ❌ |

> 💡 **Decision Tree đơn lẻ hiếm khi là lựa chọn tốt nhất** — giá trị thực sự của nó là làm nền tảng cho **Bagging, Random Forest, Boosting** (Bài 19, 20, 21).

---

## 6. Thực hành trong R

### Cài đặt thư viện

```r
install.packages(c("rpart", "rpart.plot", "caret", "pROC", "tidyverse", "vip"))

library(tidyverse)
library(rpart)        # Xây dựng Decision Tree (CART)
library(rpart.plot)   # Visualize cây đẹp hơn
library(caret)        # Tuning + CV (nhất quán Bài 17)
library(pROC)
library(vip)
```

---

### 6.1. Classification Tree — Titanic

#### Nạp và tiền xử lý

```r
titanic <- read.csv("titanic.csv", stringsAsFactors = TRUE)

titanic_clean <- titanic %>%
  select(survived, pclass, sex, age, sibsp, parch, fare) %>%
  mutate(survived = factor(survived, levels = c(0, 1),
                           labels = c("No", "Yes"))) %>%
  drop_na()

set.seed(42)
train_idx  <- createDataPartition(titanic_clean$survived, p = 0.8, list = FALSE)
tit_train  <- titanic_clean[train_idx, ]
tit_test   <- titanic_clean[-train_idx, ]

cat("Train:", nrow(tit_train), "| Test:", nrow(tit_test), "\n")
prop.table(table(tit_train$survived))
```

#### Cây mặc định

```r
set.seed(42)
tree_default <- rpart(
  survived ~ .,
  data   = tit_train,
  method = "class"    # "class" cho phân loại, "anova" cho hồi quy
)

# Visualize
rpart.plot(
  tree_default,
  type    = 4,         # Kiểu vẽ (0–5, type=4 hiển thị rõ nhất)
  extra   = 104,       # Hiển thị xác suất và % quan sát
  under   = TRUE,
  fallen.leaves = TRUE,
  main    = "Decision Tree — Titanic (Mặc định)"
)
```

**Đọc mỗi nút trong cây:**
- Dòng 1: Nhãn dự đoán (lớp chiếm đa số)
- Dòng 2: Xác suất của lớp "Yes"
- Dòng 3: % quan sát rơi vào nút này

```r
# Xem cấu trúc dạng text
print(tree_default)

# Xem bảng cp (complexity parameter)
printcp(tree_default)
plotcp(tree_default)   # Vẽ cross-validation error theo cp
```

#### Đánh giá mô hình

```r
# Dự đoán nhãn
pred_class <- predict(tree_default, newdata = tit_test, type = "class")

# Dự đoán xác suất (để tính AUC)
pred_prob <- predict(tree_default, newdata = tit_test, type = "prob")[, "Yes"]

# Confusion matrix
cm <- confusionMatrix(pred_class, tit_test$survived, positive = "Yes")
print(cm)

# AUC
roc_tree <- roc(tit_test$survived, pred_prob,
                levels = c("No", "Yes"), quiet = TRUE)
cat("AUC (Decision Tree mặc định):", round(auc(roc_tree), 4), "\n")
```

---

### 6.2. Ảnh hưởng của độ sâu cây (Depth)

```r
# Huấn luyện cây ở nhiều độ sâu khác nhau
depths   <- 1:10
acc_train <- numeric(length(depths))
acc_test  <- numeric(length(depths))

for (i in seq_along(depths)) {
  tree_d <- rpart(survived ~ ., data = tit_train, method = "class",
                  control = rpart.control(maxdepth = depths[i], cp = 0))

  acc_train[i] <- mean(predict(tree_d, tit_train, type = "class") == tit_train$survived)
  acc_test[i]  <- mean(predict(tree_d, tit_test,  type = "class") == tit_test$survived)
}

# Visualize bias-variance tradeoff
data.frame(depth = depths, Train = acc_train, Test = acc_test) %>%
  pivot_longer(-depth, names_to = "Set", values_to = "Accuracy") %>%
  ggplot(aes(x = depth, y = Accuracy, color = Set, linetype = Set)) +
  geom_line(linewidth = 1) +
  geom_point(size = 2) +
  scale_color_manual(values = c("Train" = "steelblue", "Test" = "tomato")) +
  labs(title  = "Bias–Variance Tradeoff — Decision Tree (Titanic)",
       subtitle = "Train Accuracy vs Test Accuracy theo độ sâu cây",
       x = "Độ sâu cây (maxdepth)", y = "Accuracy") +
  theme_minimal()
```

> 💡 **Quan sát:**
> - Depth tăng → Train Accuracy tăng liên tục (cây "học thuộc").
> - Test Accuracy tăng rồi **giảm** — điểm giảm là dấu hiệu overfitting bắt đầu.
> - Depth tối ưu là nơi Test Accuracy đạt cao nhất.

---

### 6.3. Pruning với Cross-Validation — Chọn `cp` tối ưu

#### Cách 1: Dùng `rpart` trực tiếp

```r
# Xây cây đầy đủ (cp = 0)
set.seed(42)
tree_full <- rpart(survived ~ ., data = tit_train, method = "class",
                   control = rpart.control(cp = 0, minsplit = 2))

# Bảng cp và CV error
cp_table <- as.data.frame(tree_full$cptable)
print(cp_table)

# Vẽ CV error theo cp
plotcp(tree_full, main = "Cross-Validation Error theo Complexity Parameter (cp)")

# Tìm cp tối ưu: cp có xerror nhỏ nhất
best_cp <- cp_table$CP[which.min(cp_table$xerror)]
cat("cp tối ưu:", round(best_cp, 6), "\n")

# Cắt tỉa cây
tree_pruned <- prune(tree_full, cp = best_cp)

rpart.plot(tree_pruned, type = 4, extra = 104, fallen.leaves = TRUE,
           main = paste("Cây sau Pruning (cp =", round(best_cp, 4), ")"))

# So sánh kết quả
pred_pruned <- predict(tree_pruned, tit_test, type = "class")
acc_full    <- mean(predict(tree_full,   tit_test, type = "class") == tit_test$survived)
acc_pruned  <- mean(pred_pruned == tit_test$survived)

cat(sprintf("Accuracy — Cây đầy đủ:  %.4f\n", acc_full))
cat(sprintf("Accuracy — Sau pruning: %.4f\n", acc_pruned))
```

#### Cách 2: Dùng `caret` (nhất quán Bài 17)

```r
ctrl <- trainControl(
  method          = "cv",
  number          = 10,
  classProbs      = TRUE,
  summaryFunction = twoClassSummary,
  savePredictions = "final"
)

# Grid search cp
cp_grid <- expand.grid(cp = c(0, 0.001, 0.005, 0.01, 0.02, 0.05, 0.1))

set.seed(42)
tree_caret <- train(
  survived ~ .,
  data      = tit_train,
  method    = "rpart",
  trControl = ctrl,
  tuneGrid  = cp_grid,
  metric    = "ROC"
)

# Kết quả theo từng cp
print(tree_caret$results[, c("cp", "ROC", "Sens", "Spec")])
plot(tree_caret, main = "Chọn cp tối ưu qua 10-Fold CV (caret)")

cat("cp tối ưu (caret):", tree_caret$bestTune$cp, "\n")
cat("AUC tốt nhất:     ", round(max(tree_caret$results$ROC), 4), "\n")
```

---

### 6.4. Visualize cây tốt nhất và Feature Importance

```r
# Vẽ cây sau khi pruning (dùng kết quả từ caret)
best_tree_model <- tree_caret$finalModel
rpart.plot(
  best_tree_model,
  type          = 4,
  extra         = 104,
  fallen.leaves = TRUE,
  box.palette   = "RdYlGn",
  shadow.col    = "gray",
  main          = "Decision Tree tối ưu — Titanic"
)

# Feature Importance
importance_df <- data.frame(
  variable   = names(best_tree_model$variable.importance),
  importance = best_tree_model$variable.importance
) %>%
  arrange(desc(importance))

ggplot(importance_df, aes(x = reorder(variable, importance), y = importance)) +
  geom_col(fill = "steelblue") +
  coord_flip() +
  labs(title = "Feature Importance — Decision Tree (Titanic)",
       x = NULL, y = "Importance (Gini Decrease)") +
  theme_minimal()
```

---

### 6.5. Regression Tree — Wines Quality

Dự đoán **điểm chất lượng rượu** (`quality` — biến liên tục).

```r
wines <- read.csv("wines.csv", stringsAsFactors = TRUE)

# Dùng các biến hóa học để dự đoán quality
wines_reg <- wines %>% select(-type)

# Chia train/test bằng caret
set.seed(42)
w_idx   <- createDataPartition(wines_reg$quality, p = 0.8, list = FALSE)
w_train <- wines_reg[w_idx, ]
w_test  <- wines_reg[-w_idx, ]

# Regression Tree (method = "anova")
tree_reg <- rpart(
  quality ~ .,
  data    = w_train,
  method  = "anova",
  control = rpart.control(cp = 0.01)
)

rpart.plot(
  tree_reg,
  type          = 4,
  extra         = 101,    # Hiển thị số quan sát tại mỗi nút
  fallen.leaves = TRUE,
  main          = "Regression Tree — Wine Quality"
)

# Đánh giá
pred_reg <- predict(tree_reg, newdata = w_test)

rmse_tree <- sqrt(mean((w_test$quality - pred_reg)^2))
mae_tree  <- mean(abs(w_test$quality - pred_reg))
r2_tree   <- 1 - sum((w_test$quality - pred_reg)^2) /
                  sum((w_test$quality - mean(w_test$quality))^2)

cat(sprintf("RMSE: %.4f\n", rmse_tree))
cat(sprintf("MAE:  %.4f\n", mae_tree))
cat(sprintf("R²:   %.4f\n", r2_tree))

# So sánh với Linear Regression
lm_wine <- lm(quality ~ ., data = w_train)
pred_lm  <- predict(lm_wine, newdata = w_test)

rmse_lm <- sqrt(mean((w_test$quality - pred_lm)^2))
r2_lm   <- 1 - sum((w_test$quality - pred_lm)^2) /
                sum((w_test$quality - mean(w_test$quality))^2)

cat(sprintf("\nSo sánh:\n"))
cat(sprintf("  Regression Tree — RMSE: %.4f | R²: %.4f\n", rmse_tree, r2_tree))
cat(sprintf("  Linear Regression—RMSE: %.4f | R²: %.4f\n", rmse_lm,   r2_lm))
```

**Pruning cho Regression Tree:**

```r
# Cây đầy đủ rồi tìm cp tối ưu
set.seed(42)
tree_reg_full <- rpart(quality ~ ., data = w_train, method = "anova",
                       control = rpart.control(cp = 0))

plotcp(tree_reg_full, main = "CV Error theo cp — Wine Quality")

best_cp_reg     <- tree_reg_full$cptable[which.min(tree_reg_full$cptable[, "xerror"]), "CP"]
tree_reg_pruned <- prune(tree_reg_full, cp = best_cp_reg)

pred_pruned_reg <- predict(tree_reg_pruned, newdata = w_test)
rmse_pruned     <- sqrt(mean((w_test$quality - pred_pruned_reg)^2))

cat(sprintf("RMSE — Cây đầy đủ:  %.4f\n",
            sqrt(mean((w_test$quality - predict(tree_reg_full, w_test))^2))))
cat(sprintf("RMSE — Sau pruning: %.4f\n", rmse_pruned))
```

---

### 6.6. So sánh toàn diện: Decision Tree vs Logistic Regression

#### German Credit — Classification

```r
german <- read.csv("german1.csv")
german$target <- factor(german$target, levels = c(1, 2),
                        labels = c("Good", "Bad"))

# Chia train/test bằng caret
set.seed(42)
g_idx   <- createDataPartition(german$target, p = 0.8, list = FALSE)
g_train <- german[g_idx, ]
g_test  <- german[-g_idx, ]

cat("German — Train:", nrow(g_train), "| Test:", nrow(g_test), "\n")

ctrl_compare <- trainControl(
  method = "cv", number = 10,
  classProbs = TRUE, summaryFunction = twoClassSummary
)

# Logistic Regression
set.seed(42)
m_lr <- train(target ~ ., data = g_train, method = "glm",
              family = "binomial", trControl = ctrl_compare, metric = "ROC")

# Decision Tree (tuning cp)
set.seed(42)
m_tree <- train(target ~ ., data = g_train, method = "rpart",
                trControl = ctrl_compare, metric = "ROC",
                tuneGrid = expand.grid(cp = c(0, 0.001, 0.005, 0.01, 0.02, 0.05)))

# Tổng hợp kết quả CV
results <- resamples(list(
  LogisticRegression = m_lr,
  DecisionTree       = m_tree
))

summary(results, metric = "ROC")
dotplot(results, metric = "ROC",
        main = "So sánh AUC (10-Fold CV) — German Credit")

# Đánh giá trên test set
get_auc <- function(model, test, truth) {
  p       <- predict(model, newdata = test, type = "prob")[, "Bad"]
  roc_obj <- roc(test[[truth]], p, levels = c("Good", "Bad"), quiet = TRUE)
  round(auc(roc_obj), 4)
}

cat("AUC trên TEST SET:\n")
cat("  Logistic Regression:", get_auc(m_lr,   g_test, "target"), "\n")
cat("  Decision Tree:      ", get_auc(m_tree, g_test, "target"), "\n")
```

#### Wines — Nhận diện ranh giới phi tuyến

```r
# Dự đoán type (red/white) — ranh giới trong không gian hóa học
wines_clf       <- wines %>% select(-quality)
wines_clf$type  <- factor(wines_clf$type)

set.seed(42)
wc_idx   <- createDataPartition(wines_clf$type, p = 0.8, list = FALSE)
wc_train <- wines_clf[wc_idx, ]
wc_test  <- wines_clf[-wc_idx, ]

ctrl_w <- trainControl(method = "cv", number = 5,
                       classProbs = TRUE, summaryFunction = twoClassSummary)

set.seed(42)
m_lr_w <- train(type ~ ., data = wc_train, method = "glm",
                family = "binomial", trControl = ctrl_w, metric = "ROC")

set.seed(42)
m_tree_w <- train(type ~ ., data = wc_train, method = "rpart",
                  trControl = ctrl_w, metric = "ROC",
                  tuneGrid = expand.grid(cp = c(0, 0.001, 0.005, 0.01)))

cat("\n=== Wines — Phân loại red/white ===\n")
cat("Logistic Regression AUC (CV):",
    round(max(m_lr_w$results$ROC), 4), "\n")
cat("Decision Tree AUC (CV):      ",
    round(max(m_tree_w$results$ROC), 4), "\n")
```

> 💡 **Nhận xét dự kiến:** Với Wines, Decision Tree thường cho kết quả tương đương hoặc tốt hơn Logistic Regression vì ranh giới giữa red/white wine **không tuyến tính** trong không gian hóa học.

---

### 6.7. Medical Care — Cây lớn và biến hỗn hợp

```r
medical <- read.csv("medical_care.csv", stringsAsFactors = TRUE)

medical_clean <- medical %>%
  select(UCURNINS, UMARSTAT, USATMED, REGION, FHOSP, FDENT, FEMER,
         FDOCT, UIMMSTAT, UAGE, U_FTPT, U_WKSLY, UBRACE, GENDER, UEDUC3) %>%
  drop_na() %>%
  mutate(UCURNINS = factor(UCURNINS))

# Chia train/test bằng caret
set.seed(42)
med_idx   <- createDataPartition(medical_clean$UCURNINS, p = 0.8, list = FALSE)
med_train <- medical_clean[med_idx, ]
med_test  <- medical_clean[-med_idx, ]

# Decision Tree với cp được tuned
ctrl_med <- trainControl(
  method = "cv", number = 5,
  classProbs = TRUE, summaryFunction = twoClassSummary
)

set.seed(42)
tree_med <- train(
  UCURNINS ~ .,
  data      = med_train,
  method    = "rpart",
  trControl = ctrl_med,
  metric    = "ROC",
  tuneGrid  = expand.grid(cp = c(0, 0.0001, 0.0005, 0.001, 0.005, 0.01))
)

cat("cp tối ưu (Medical):", tree_med$bestTune$cp, "\n")
cat("AUC (CV):           ", round(max(tree_med$results$ROC), 4), "\n")

# Test set
pred_med <- predict(tree_med, newdata = med_test, type = "prob")[, "Yes"]
roc_med  <- roc(med_test$UCURNINS, pred_med, levels = c("No", "Yes"), quiet = TRUE)
cat("AUC (Test set):     ", round(auc(roc_med), 4), "\n")

# So sánh với Logistic Regression
lr_med <- glm(UCURNINS ~ UMARSTAT + USATMED + REGION + FHOSP + FDENT +
                FEMER + FDOCT + UIMMSTAT + UAGE + U_FTPT + U_WKSLY +
                UBRACE + GENDER + UEDUC3,
              data = med_train, family = binomial())

pred_lr_med <- predict(lr_med, newdata = med_test, type = "response")
roc_lr_med  <- roc(med_test$UCURNINS, pred_lr_med,
                   levels = c("No", "Yes"), quiet = TRUE)

cat("\n--- So sánh trên Test Set (Medical) ---\n")
cat("Logistic Regression AUC:", round(auc(roc_lr_med), 4), "\n")
cat("Decision Tree AUC:      ", round(auc(roc_med),    4), "\n")

# Vẽ cây tốt nhất
rpart.plot(tree_med$finalModel, type = 4, extra = 104, fallen.leaves = TRUE,
           main = "Decision Tree tối ưu — Medical Care",
           box.palette = "RdBu")
```

---

### 6.8. Tổng hợp so sánh tất cả dataset

```r
summary_results <- data.frame(
  Dataset  = c("Titanic", "German Credit", "Wines (type)", "Medical Care"),
  AUC_Tree = c(
    round(max(tree_caret$results$ROC), 4),
    round(max(m_tree$results$ROC),     4),
    round(max(m_tree_w$results$ROC),   4),
    round(max(tree_med$results$ROC),   4)
  ),
  AUC_LR = c(
    round(max(m_lr$results$ROC),   4),
    round(max(m_lr$results$ROC),   4),
    round(max(m_lr_w$results$ROC), 4),
    NA
  )
)

print(summary_results)
```

---

## 7. Ưu điểm và Hạn chế

### Ưu điểm
- **Dễ giải thích và trực quan hóa** — có thể vẽ thành sơ đồ cây.
- **Không cần tiền xử lý** — không cần chuẩn hóa, xử lý được cả biến số lẫn biến phân loại.
- **Tự động nắm bắt tương tác biến và quan hệ phi tuyến.**
- **Bền vững với outlier** — phân chia dựa trên thứ tự, không phải giá trị tuyệt đối.
- **Nền tảng của các mô hình mạnh** — Random Forest, Gradient Boosting.

### Hạn chế
- **Variance cao** — thay đổi nhỏ trong dữ liệu có thể tạo ra cây hoàn toàn khác.
- **Thiên kiến với biến có nhiều giá trị** — biến có nhiều unique values được ưu tiên phân chia.
- **Ranh giới trực giao** — không hiệu quả khi ranh giới thực sự là đường chéo.
- **Hiệu năng thường kém hơn** các mô hình ensemble (Random Forest, XGBoost) khi đứng một mình.

---

## 8. Bài tập

### Bài tập 1 — Titanic (`titanic.csv`)

1. Xây dựng Classification Tree với các `maxdepth` = 2, 4, 6, 8, 10. Vẽ biểu đồ Train/Test Accuracy theo depth và xác định điểm overfitting bắt đầu.
2. Dùng `rpart` để tìm `cp` tối ưu qua bảng `cptable`. Vẽ cây sau pruning và giải thích từng nút.
3. Dùng `caret` (10-Fold CV) để tuning `cp`. So sánh `cp` tốt nhất với kết quả từ câu 2.
4. So sánh Feature Importance của Decision Tree với Logistic Regression từ Bài 17 — hai mô hình có đồng thuận về biến quan trọng nhất không?

### Bài tập 2 — Wines (`wines.csv`)

1. Xây dựng **Regression Tree** dự đoán `quality`. Tìm `cp` tối ưu và vẽ cây sau pruning.
2. So sánh RMSE và R² giữa: Regression Tree, Linear Regression.
3. Xây dựng **Classification Tree** phân loại `type` (red/white). AUC có cao hơn Logistic Regression không? Giải thích tại sao.
4. *(Nâng cao)* Với Regression Tree, vẽ biểu đồ **predicted vs actual** cho cả hai mô hình. Mô hình nào có phân phối sai số tốt hơn?

### Bài tập 3 — German Credit (`german1.csv`)

1. Chạy 10-Fold CV với cả Logistic Regression và Decision Tree. Dùng `resamples()` để so sánh phân phối AUC của hai mô hình.
2. Với Decision Tree tốt nhất, vẽ cây và giải thích: biến nào quan trọng nhất trong việc đánh giá rủi ro tín dụng?
3. *(Nâng cao)* Kiểm tra: Decision Tree có xu hướng thiên lệch với lớp nào (Good hay Bad)? Thử dùng tham số `prior` trong `rpart` để điều chỉnh.

### Bài tập 4 — Medical Care (`medical_care.csv`)

1. Dùng `caret` để tuning `cp` đồng thời qua 5-Fold CV với nhiều giá trị cp hơn.
2. So sánh AUC trên test set: Logistic Regression (Bài 17) vs Decision Tree (Bài 18).
3. Cây tối ưu có bao nhiêu nút lá? Vẽ và giải thích cây — quy luật phân loại có ý nghĩa thực tế không?

---

## 9. Tài liệu tham khảo

- **rpart package:** Therneau, T., & Atkinson, B. (2023). *rpart: Recursive Partitioning and Regression Trees*. CRAN.
- **rpart.plot package:** Milborrow, S. (2023). *rpart.plot: Plot 'rpart' Models*. CRAN.
- **caret package:** Kuhn, M. (2023). *caret: Classification and Regression Training*. CRAN. <https://topepo.github.io/caret/>
- Breiman, L., Friedman, J., Stone, C. J., & Olshen, R. A. (1984). *Classification and Regression Trees*. CRC Press. *(Cuốn gốc của CART)*
- James, G. et al. (2021). *An Introduction to Statistical Learning* (2nd ed.). Ch. 8: Tree-Based Methods.
- Hastie, T. et al. (2009). *The Elements of Statistical Learning* (2nd ed.). Ch. 9: Additive Models, Trees, and Related Methods.

---

*Bài giảng được soạn cho môn Data Science với R — Bài 18: Decision Tree*  
*Tiếp theo: Bài 19 — Bagging & Random Forest*
